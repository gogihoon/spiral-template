# Spec: <기능명>

> 구현 전에 작성하고 승인받는다. 코드는 아직 쓰지 않는다.
> 이 폴더(example-feature)는 예시다. 새 기능은 docs/features/<기능명>/ 에 같은 양식으로 만든다.

## 위험 / 불확실성
이 기능에서 가장 불확실한 것을 먼저 적는다. 리뷰가 어디를 집중해서 볼지의 기준.

| 위험 | 검증 방법 | 코드로만 확인 가능? |
|---|---|---|
|  |  | 아니오 |

> "코드로만 확인 가능"이 **예**이면, 추측으로 스펙을 쓰지 말고 최소 코드로 사실을 먼저 확인한 뒤 결과를 아래 설계에 반영한다. 확인용 코드는 사실만 남기고 버린다.

## 대상 요구사항
<FR-?, NFR-?>

## 구현 방식과 이유
<왜 이 접근을 택했는가 + 검토했지만 버린 대안>

## 파일/컴포넌트 구조
<어떤 파일이 새로 생기고/바뀌는가>

## 엣지케이스
- 

## 이번에 하지 않을 것
- 

## 함수 명세
주요 함수마다 아래를 보고한다.

### `함수명(params) -> 반환타입`
- 역할: <한 문장>
- 입력:
  | 인자 | 타입 | 제약 / 유효범위 |
  |---|---|---|
  |  |  |  |
- 출력: <타입과 의미>
- 예외 / 에러 케이스: <어떤 입력에서 무엇을>
- 부작용(side effect): <DB 쓰기, 외부 API 호출, 상태 변경 등>
- 호출 관계: <호출하는 것 / 호출되는 것>

## 검증 계획
DoD와 위험을 테스트 케이스로 번역한다. 구현(③)은 이 테스트 통과로 완료를 판정한다.

| 검증 대상 (DoD/위험) | 테스트 케이스 | 기대 결과 |
|---|---|---|
|  |  |  |
